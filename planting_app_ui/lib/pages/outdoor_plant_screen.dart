import 'package:flutter/material.dart';

class OutdoorPlantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'Outdoor Plant',
          style: TextStyle(fontSize: 40.0),
        ),
      ),
    );
  }
}
